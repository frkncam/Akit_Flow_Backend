# Grafana datasource provisioning — Tempo'yu otomatik bagla (elle ekleme yok).
# Konum (sunucu): /opt/akitflow/grafana/provisioning/datasources/tempo.yaml
apiVersion: 1

datasources:
  - name: Tempo
    type: tempo
    access: proxy
    url: http://tempo:3200      # docker ic ag: tempo servisi
    isDefault: true
    editable: false
